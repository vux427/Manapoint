# Rust / Cargo
target/
**/*.rs.bk
*.pdb

# Tauri build output and generated schemas
manapoint-tauri/src-tauri/gen/
manapoint-tauri/src-tauri/target/

# Node (only ever a dev-time Tauri CLI; the UI itself ships no dependencies)
node_modules/
npm-debug.log*

# Editors and OS
.vs/
.vscode/
.idea/
*.swp
*.user
.DS_Store
Thumbs.db

# Local snapshots and secrets never belong in the repo
usage-snapshot.json
settings.json
*.env
