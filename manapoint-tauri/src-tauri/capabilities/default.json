{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Panel and settings windows may call the app's own commands.",
  "windows": ["panel", "settings"],
  "permissions": ["core:default"]
}
